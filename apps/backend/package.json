{
  "name": "@secthing/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@secthing/platform": "*",
    "fastify": "^5.2.1"
  }
}

